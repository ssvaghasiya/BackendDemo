server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/indusinn_neuro
    username: indusinn_neuro_user
    password: Savan@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info
